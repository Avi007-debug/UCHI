# UCHI System Architecture

## 📊 High-Level Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER INTERFACE                          │
│                    (Browser - Any Device)                       │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                    FRONTEND APPLICATION                         │
│                  (React + TypeScript + Vite)                    │
│                                                                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐        │
│  │   Landing    │  │ Study Area   │  │    Upload    │        │
│  │     Page     │  │  Selection   │  │     Page     │        │
│  └──────────────┘  └──────────────┘  └──────────────┘        │
│                                                                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐        │
│  │   Results    │  │   Compare    │  │  Components  │        │
│  │     Page     │  │     Page     │  │    Library   │        │
│  └──────────────┘  └──────────────┘  └──────────────┘        │
│                                                                 │
│  ┌──────────────────────────────────────────────────┐         │
│  │          API Service Layer (Abstraction)         │         │
│  │  ┌─────────────────┐      ┌──────────────────┐  │         │
│  │  │   Mock API      │ OR   │   Real Backend   │  │         │
│  │  │  (Development)  │      │  (Production)    │  │         │
│  │  └─────────────────┘      └──────────────────┘  │         │
│  └──────────────────────────────────────────────────┘         │
└────────────────────────────┬────────────────────────────────────┘
                             │
                    HTTP REST API Calls
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                     BACKEND API SERVER                          │
│                    (Python Flask + CORS)                        │
│                                                                 │
│  ┌────────────────────────────────────────────────────────┐   │
│  │                    API Endpoints                       │   │
│  │  • GET  /health                                        │   │
│  │  • POST /upload-image                                  │   │
│  │  • GET  /get-results                                   │   │
│  │  • GET  /get-bangalore-summary                         │   │
│  │  • GET  /get-rvce-results                             │   │
│  │  • GET  /compare/{region}                             │   │
│  └────────────────────────────────────────────────────────┘   │
│                             │                                   │
│                             ▼                                   │
│  ┌────────────────────────────────────────────────────────┐   │
│  │              Business Logic Layer                      │   │
│  │                                                         │   │
│  │  ┌──────────────┐  ┌──────────────┐  ┌────────────┐  │   │
│  │  │     CHI      │  │   Database   │  │   Config   │  │   │
│  │  │  Generator   │  │  Operations  │  │  Settings  │  │   │
│  │  └──────────────┘  └──────────────┘  └────────────┘  │   │
│  │                                                         │   │
│  │  ┌─────────────────────────────────────────────────┐  │   │
│  │  │      AI/ML Pipeline (Future Integration)        │  │   │
│  │  │                                                  │  │   │
│  │  │  ┌─────────────┐  ┌──────────────┐  ┌────────┐ │  │   │
│  │  │  │Preprocessing│→ │  Vegetation  │→ │  CHI   │ │  │   │
│  │  │  │   Module    │  │  Detection   │  │  Calc  │ │  │   │
│  │  │  └─────────────┘  └──────────────┘  └────────┘ │  │   │
│  │  │                                                  │  │   │
│  │  │  Currently: Placeholder modules with TODOs      │  │   │
│  │  │  Future: TensorFlow/PyTorch + OpenCV            │  │   │
│  │  └─────────────────────────────────────────────────┘  │   │
│  └────────────────────────────────────────────────────────┘   │
│                             │                                   │
│                             ▼                                   │
│  ┌────────────────────────────────────────────────────────┐   │
│  │                  Data Layer                            │   │
│  │                                                         │   │
│  │  ┌──────────────┐              ┌──────────────┐       │   │
│  │  │   SQLite     │              │    File      │       │   │
│  │  │   Database   │              │   Storage    │       │   │
│  │  │              │              │              │       │   │
│  │  │ • images     │              │  uploads/    │       │   │
│  │  │ • results    │              │  *.jpg, *.png│       │   │
│  │  └──────────────┘              └──────────────┘       │   │
│  └────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Data Flow Diagram

### Current Implementation (Dummy CHI)

```
User Uploads Image
       │
       ▼
Frontend Validates
       │
       ▼
POST /upload-image
       │
       ▼
Backend Receives File
       │
       ├─→ Save file to uploads/
       │
       ├─→ Extract metadata
       │
       ├─→ Generate Dummy CHI
       │   (Region-specific ranges)
       │
       ├─→ Insert to database
       │   ├─→ image_metadata table
       │   └─→ chi_results table
       │
       ▼
Return CHI Result (JSON)
       │
       ▼
Frontend Displays
       │
       ├─→ CHI value
       ├─→ Status badge
       ├─→ Interpretation
       └─→ Metrics
```

### Future Implementation (With AI)

```
User Uploads Image
       │
       ▼
Frontend Validates
       │
       ▼
POST /upload-image
       │
       ▼
Backend Receives File
       │
       ├─→ Save file to uploads/
       │
       ▼
[1] Preprocessing
    ├─→ Resize to 512x512
    ├─→ Normalize pixels
    ├─→ Remove noise
    └─→ Enhance vegetation features
       │
       ▼
[2] Vegetation Detection (AI)
    ├─→ Load U-Net model
    ├─→ Generate vegetation mask
    ├─→ Classify healthy vs stressed
    └─→ Calculate coverage
       │
       ▼
[3] CHI Calculation
    ├─→ Calculate NDVI
    ├─→ Analyze canopy density
    ├─→ Apply weighted formula
    └─→ Region-specific calibration
       │
       ▼
Save Results to Database
       │
       ▼
Return CHI Result (JSON)
       │
       ▼
Frontend Displays Results
```

---

## 🗂️ Component Hierarchy

### Frontend Components

```
App.tsx
│
├── Layout
│   ├── Header
│   │   ├── Logo
│   │   └── Navigation
│   │       └── NavLink (×5)
│   │
│   ├── Main Content
│   │   └── Route Pages
│   │       ├── Index (Landing)
│   │       ├── StudyArea
│   │       ├── Upload
│   │       ├── Results
│   │       ├── Compare
│   │       └── NotFound
│   │
│   └── Footer
│
├── Shared Components
│   ├── CHI Components
│   │   ├── CHIDisplay
│   │   ├── CHICard
│   │   └── CHIBadge
│   │
│   ├── Charts
│   │   └── CHIBarChart
│   │
│   ├── Tables
│   │   └── RegionTable
│   │
│   ├── Comparison
│   │   └── TemporalComparisonCard
│   │
│   └── Upload
│       └── ImageUploader
│
└── UI Library (shadcn/ui)
    ├── Card, Button, Input, Select
    ├── Table, Tabs, Progress
    ├── Toast, Alert, Badge
    └── 35+ more components
```

### Backend Modules

```
app.py (Main Flask App)
│
├── Routes Layer
│   ├── /health
│   ├── /upload-image
│   ├── /get-results
│   ├── /get-bangalore-summary
│   ├── /get-rvce-results
│   └── /compare/{region}
│
├── Business Logic
│   ├── config.py
│   ├── database.py
│   └── chi_generator.py
│
└── AI Modules (Placeholders)
    ├── preprocessing.py
    ├── vegetation_detection.py
    └── chi_calculation.py
```

---

## 💾 Database Schema

```
┌─────────────────────────────────────┐
│       image_metadata                │
├─────────────────────────────────────┤
│ id (PK)           INTEGER           │
│ filename          TEXT              │
│ filepath          TEXT              │
│ area_type         TEXT              │
│ sub_region        TEXT (nullable)   │
│ date              TEXT              │
│ uploaded_at       TEXT              │
│ created_at        TIMESTAMP         │
└─────────────────────────────────────┘
                │
                │ 1:N
                ▼
┌─────────────────────────────────────┐
│         chi_results                 │
├─────────────────────────────────────┤
│ id (PK)              INTEGER        │
│ image_id (FK)        INTEGER        │
│ area_type            TEXT           │
│ sub_region           TEXT           │
│ chi_value            REAL           │
│ status               TEXT           │
│ interpretation       TEXT           │
│ date                 TEXT           │
│ vegetation_coverage  REAL           │
│ healthy_vegetation   REAL           │
│ stressed_vegetation  REAL           │
│ created_at           TIMESTAMP      │
└─────────────────────────────────────┘
```

---

## 🔀 API Mode Switching

```
┌─────────────────────────────────────┐
│      Frontend Application           │
│                                     │
│   src/services/apiConfig.ts        │
│   ┌─────────────────────────────┐  │
│   │ USE_MOCK_API = true/false   │  │
│   └─────────────┬───────────────┘  │
│                 │                   │
│        ┌────────┴────────┐         │
│        ▼                 ▼          │
│   ┌─────────┐      ┌──────────┐   │
│   │  Mock   │      │   Real   │   │
│   │   API   │      │ Backend  │   │
│   │         │      │   API    │   │
│   │ Dummy   │      │ Flask    │   │
│   │ Data    │      │ Server   │   │
│   └─────────┘      └──────────┘   │
│                          │          │
└──────────────────────────┼──────────┘
                           │
                           ▼
                    HTTP Requests
                           │
                           ▼
              ┌────────────────────────┐
              │   Backend Server       │
              │   localhost:5000       │
              └────────────────────────┘
```

---

## 📦 Deployment Architecture

```
┌──────────────────────────────────────────────────────────┐
│                    PRODUCTION                            │
└──────────────────────────────────────────────────────────┘

Frontend (Vercel/Netlify)
┌─────────────────────────┐
│  Static Files           │
│  • HTML, CSS, JS        │
│  • Images, Fonts        │
│                         │
│  Environment:           │
│  USE_MOCK_API = false   │
│  BACKEND_URL = https:// │
│     your-backend.com    │
└────────┬────────────────┘
         │
         │ HTTPS REST API
         │
         ▼
Backend (Railway/Heroku)
┌─────────────────────────┐
│  Python Flask Server    │
│  • API Endpoints        │
│  • Database (SQLite)    │
│  • File Storage         │
│                         │
│  CORS: your-frontend    │
│  Port: 5000/8000        │
└─────────────────────────┘
```

---

## 🎨 UI Component Styling

```
Theme System (Tailwind CSS)
│
├── Colors
│   ├── Primary: Green (Nature theme)
│   ├── Secondary: Earth tones
│   ├── Accent: Fresh green
│   ├── CHI Colors:
│   │   ├── Excellent: Dark green
│   │   ├── Good: Light green
│   │   ├── Moderate: Orange
│   │   ├── Poor: Red
│   │   └── Critical: Dark red
│   └── Neutrals: Grays
│
├── Typography
│   ├── Font Family: Inter
│   ├── Display: font-display
│   └── Body: font-sans
│
├── Spacing
│   └── Tailwind scale (0.25rem increments)
│
└── Components
    ├── Cards: gradient-card
    ├── Buttons: variant="nature"
    └── Badges: status colors
```

---

## 🔒 Security Considerations

```
Current (Development):
┌────────────────────────────┐
│ • CORS: Allow all origins  │
│ • No authentication        │
│ • Local file storage       │
│ • No rate limiting         │
└────────────────────────────┘

Production Recommendations:
┌────────────────────────────┐
│ • CORS: Specific domain    │
│ • User authentication      │
│ • Cloud storage (S3)       │
│ • Rate limiting (Redis)    │
│ • File validation          │
│ • SQL injection prevention│
│ • HTTPS only               │
│ • Environment variables    │
└────────────────────────────┘
```

---

## 📊 Performance Optimization

```
Frontend:
┌─────────────────────────┐
│ • Code splitting        │
│ • Lazy loading routes   │
│ • Image optimization    │
│ • Bundle minimization   │
│ • React.memo for heavy  │
│   components            │
└─────────────────────────┘

Backend:
┌─────────────────────────┐
│ • Database indexing     │
│ • Caching (Redis)       │
│ • Async processing      │
│ • Connection pooling    │
│ • Image compression     │
│ • CDN for static files  │
└─────────────────────────┘
```

---

**This architecture ensures:**
- ✅ Scalability
- ✅ Maintainability
- ✅ Testability
- ✅ Production-readiness
- ✅ AI integration capability

